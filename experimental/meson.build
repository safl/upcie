# Experimental subprojects: out-of-tree kernel modules and the tests that drive
# them. Nothing here is part of the uPCIe API and none of it builds by default.
# Each module has to be built and loaded separately, as a DKMS package or by
# hand, before its test can run. See the README in each directory.
subdir('iommu_map_pa')
