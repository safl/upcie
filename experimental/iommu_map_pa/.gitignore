# Debian packaging build output
debian/.debhelper/
debian/files
debian/*.substvars
debian/iommu-map-pa-dkms/
debian/debhelper-build-stamp
# Note: *.debhelper covers the generated *.dkms.debhelper, but not the
# hand-written debian/iommu-map-pa-dkms.dkms that dh_dkms reads.
debian/*.debhelper
debian/*.debhelper.log
*.deb
*.buildinfo
*.changes
